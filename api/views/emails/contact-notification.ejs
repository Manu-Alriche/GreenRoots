<!-- views/emails/contact-notification.ejs -->
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            line-height: 1.6; 
            color: #333; 
            margin: 0; 
            padding: 0; 
            background-color: #f5f5f5;
        }
        .container { 
            max-width: 600px; 
            margin: 20px auto; 
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .header { 
            background: linear-gradient(135deg, #16a085, #27ae60); 
            color: white; 
            padding: 30px 20px; 
            text-align: center;
        }
        .header h2 {
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }
        .content { 
            padding: 30px;
        }
        .field { 
            margin-bottom: 20px;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 8px;
            border-left: 4px solid #16a085;
        }
        .label { 
            font-weight: 600; 
            color: #16a085;
            display: block;
            margin-bottom: 5px;
        }
        .value {
            color: #333;
        }
        .message-content { 
            background: white; 
            padding: 20px; 
            border: 2px solid #e9ecef;
            border-radius: 8px;
            margin-top: 10px;
            white-space: pre-wrap;
        }
        .footer { 
            text-align: center; 
            padding: 20px;
            background: #f8f9fa;
            font-size: 14px; 
            color: #666; 
            border-top: 1px solid #e9ecef;
        }
        .email-link {
            color: #16a085;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>📨 Nouveau message de contact</h2>
            <p>Message reçu depuis GreenRoots</p>
        </div>
        <div class="content">
            <div class="field">
                <span class="label">👤 Nom complet</span>
                <div class="value"><%= prenom %> <%= nom %></div>
            </div>
            <div class="field">
                <span class="label">📧 Email</span>
                <div class="value">
                    <a href="mailto:<%= email %>" class="email-link"><%= email %></a>
                </div>
            </div>
            <div class="field">
                <span class="label">📅 Date</span>
                <div class="value"><%= date %></div>
            </div>
            <div class="field">
                <span class="label">💬 Message</span>
                <div class="message-content"><%= content %></div>
            </div>
        </div>
        <div class="footer">
            <p>Message envoyé depuis le formulaire de contact de <strong>GreenRoots</strong></p>
        </div>
    </div>
</body>
</html>